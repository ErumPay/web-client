# web-client

## 실행 방법

```bash
npm install
npm run dev
```

개발 서버 기본 주소는 `http://localhost:5173`입니다.

## 빌드

```bash
npm run build
```

## 환경변수

로컬 실행 설정은 `.env`에서 관리합니다. 브라우저에 노출되는 값은 `VITE_` 접두사를 사용합니다.
